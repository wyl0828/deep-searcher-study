# Ragent 对照工程化优化收口

## 实施提交

- `8cd44e7`：PostgreSQL 产品数据库与 Alembic 启动校验
- `8b5e993`：local/S3 统一文档对象存储
- `f78bf55`：可选 RocketMQ 事务入库与 Consumer
- `e0503fd`：有序 Chat fallback、有效首包和进程内熔断
- `fdbfe2f`：持久化滚动会话摘要与 Redis 摘要锁

## 自动回归

每阶段均通过 Fast Gate。最终功能阶段结果：

- Python：986 passed，11 skipped
- 前端：35 passed
- Playwright E2E：2 passed
- Alembic：空库升级到 `20260813_0015`
- Trust consistency：58/58
- Citation span：8/8
- Risk profile：14/14
- Trust provenance：23/23
- 冻结检索、上下文、回答报告契约校验通过

## 真实依赖验证

| 依赖 | 真实验证结果 |
| --- | --- |
| PostgreSQL 16 | 空 `public` Schema 全量 Alembic 升级、Schema head 校验和 `SKIP LOCKED` 任务抢占通过 |
| MinIO / S3 | 上传对象后重启 MinIO，原文流和 Worker 临时 materialize 均可读取 |
| RocketMQ 5.3.2 | Commit、Rollback、Broker 回查、未 ACK 重试、ACK 后停止重投、长耗时续租通过 |
| Redis 7 | 同一用户+会话锁首次获取成功、第二实例获取失败、释放后可恢复 |

## 未完成的组合验收

本机没有执行以下完整拓扑：两个 API、两个 RocketMQ Consumer、共享 PostgreSQL、Redis、S3 和
Milvus 的一体化故障切换。各基础设施与核心并发语义已经分别真实验证，但不能等价为完整集群验收，
因此 README 和简历不得写“高可用集群已落地”或“端到端 Exactly Once”。
