# Copy this file to local.config.ps1 and fill in the current temporary node.
# local.config.ps1 is ignored by Git and must never contain a private key value.
$DeepSearcherServer = ""
$DeepSearcherSshKey = ""
$DeepSearcherRemoteRoot = "/opt/deepsearcher-study"
